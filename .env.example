#################################
# App
#################################
APP_NAME=Aparca
APP_ENV=local              # 👉 en prod: "production"
APP_KEY=                   # 👉 se genera con: php artisan key:generate
APP_DEBUG=true             # 👉 en prod: "false"
APP_URL=http://localhost   # 👉 en prod: tu dominio https://aparca.tu-dominio.com

# Idiomas
APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_ES

#################################
# Logging
#################################
LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=debug            # 👉 en prod: "warning" o "error"
LOG_DEPRECATIONS_CHANNEL=null

#################################
# Database
#################################
DB_CONNECTION=mysql
DB_HOST=127.0.0.1          # 👉 en Docker: nombre del servicio, ej. "mariadb"
DB_PORT=3306
DB_DATABASE=aparca
DB_USERNAME=aparca
DB_PASSWORD=               # 👉 en prod: usa una contraseña segura

#################################
# Cache / Queue / Session
#################################
CACHE_DRIVER=file          # 👉 en prod: "redis" o "memcached"
QUEUE_CONNECTION=sync      # 👉 en prod: "database" o "redis"
SESSION_DRIVER=file        # 👉 en prod: "database" o "redis"
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_DOMAIN=null         # 👉 en prod: dominio principal, ej. ".aparca.com"

#################################
# Redis (opcional)
#################################
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null         # 👉 en prod: usa contraseña segura si Redis está expuesto
REDIS_PORT=6379

#################################
# CORS (para Angular / frontends)
#################################
CORS_ALLOWED_ORIGINS=http://localhost:4200
# 👉 en prod: pon todos los dominios front permitidos separados por comas
# ej: https://app.aparca.com,https://admin.aparca.com

#################################
# JWT (php-open-source-saver/jwt-auth)
#################################
JWT_SECRET=                # 👉 se genera con: php artisan jwt:secret
JWT_TTL=1440               # 1 día en minutos
JWT_REFRESH_TTL=129600      # 90 días en minutos

#################################
# Mail
#################################
MAIL_MAILER=log            # 👉 en prod: "smtp" o el driver que uses
MAIL_HOST=127.0.0.1        # 👉 en prod: host SMTP
MAIL_PORT=2525             # 👉 en prod: puerto SMTP (587 / 465 típicamente)
MAIL_USERNAME=null         # 👉 en prod: usuario SMTP
MAIL_PASSWORD=null         # 👉 en prod: contraseña SMTP
MAIL_ENCRYPTION=null       # 👉 en prod: tls/ssl
MAIL_FROM_ADDRESS="no-reply@aparca.local"   # 👉 en prod: correo real
MAIL_FROM_NAME="${APP_NAME}"

#################################
# Filesystem
#################################
FILESYSTEM_DISK=local      # 👉 en prod: "s3", "gcs", etc.

#################################
# Otros
#################################
VITE_APP_NAME="${APP_NAME}"
